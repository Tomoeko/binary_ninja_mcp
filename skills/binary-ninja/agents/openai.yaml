interface:
  display_name: "Binary Ninja"
  short_description: "Analyze binaries through Binary Ninja MCP"
  default_prompt: "Use $binary-ninja to analyze this binary and explain the relevant functions and data flow."
dependencies:
  tools:
    - type: "mcp"
      value: "binary_ninja"
      description: "Headless Binary Ninja analysis and decompilation tools"
